// Bridge for future native features (receipt printer, barcode scanner).
// Kept minimal for now — the renderer talks to the backend over HTTP.
window.addEventListener("DOMContentLoaded", () => {
  // no-op placeholder
});
